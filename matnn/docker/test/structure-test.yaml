schemaVersion: "2.0.0"

commandTests:
  - name: "python exists"
    command: "python3"
    args: ["--version"]
    expectedOutput:
      - "Python"

fileExistenceTests:
- name: 'fastapi'
  path: '/fastapi/main.py'
  shouldExist: true
  permissions: '-rw-r--r--'
  uid: 1000
  gid: 1000
  # isExecutableBy: 'group'

metadataTest:
  exposedPorts: ["8000", "8090", "9090"]
  entrypoint:  ["/tini", "--"]
  cmd: ["/usr/bin/supervisord"]
  # workdir: "/"
  # user: "web"
